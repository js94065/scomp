// recursive.decaf

class Program {

	int factorial(int n) {
		if (n <= 0) {
			return 1;
		}

		return n * factorial(n - 1);
	}

	void main() {
		int n;

		n = 0;

		callout("printf", "%d! = %d\n", n, factorial(n));

		n = 5;

		callout("printf", "%d! = %d\n", n, factorial(n));
	}

}
